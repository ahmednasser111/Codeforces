// Magical Sticks
#include <bits\stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
  int t, n;
  scanf("%d", &t);
  while (t--)
  {
    scanf("%d", &n);
    cout << (long long)ceil(n / 2.0) << '\n';
  }
  return 0;
}
