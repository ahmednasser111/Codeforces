#include <bits\stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
    int n, m, k;
    cin >> n >> m >> k;
    if (m >= n && k >= n)
    {
        cout << "YES";
    }
    else
        cout << "NO";

    return 0;
}
