#include <bits\stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
    int t;
    long n;
    cin >> t;
    while (t--)
    {
        cin >> n;
        cout << (n - 1) / 2 << "\n";
    }
    return 0;
}
