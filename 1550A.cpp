// Find The Array
#include <bits\stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
  int t;
  scanf("%d", &t);
  while (t--)
  {
    int s;
    scanf("%d", &s);
    cout << ceil(sqrt(s)) << '\n';
  }
}
