// Suborrays
#include <bits\stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
  int t;
  scanf("%d", &t);
  while (t--)
  {
    int n;
    scanf("%d", &n);
    for (int i = n; i > 0; i--)
      cout << i << " ";
    cout << endl;
  }
}
