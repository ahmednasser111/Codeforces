// XOR Mixup
#include <bits\stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
  int t;
  scanf("%d", &t);
  while (t--)
  {
    int n, x;
    scanf("%d", &n);
    for (int i = 0; i < n; i++)
      scanf("%d", &x);
    cout << x << endl;
  }
}